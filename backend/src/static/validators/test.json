{
    "$jsonSchema": {
        "bsonType": "object",
        "required": ["required_string","required_bool"],
        "properties": {
            "required_string": {
                "bsonType": "string",
                "description": "this is a required string",
                "uniqueItems": true
            },
            "required_bool": {
                "bsonType": "bool",
                "description": "this is a required boolean"
            }
        }
    }
}